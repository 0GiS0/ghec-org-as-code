# Server Configuration
SERVER_PORT=8080

# Database Configuration (PostgreSQL for production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=demo
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Spring Profiles
SPRING_PROFILES_ACTIVE=dev

# Logging
LOG_LEVEL=DEBUG

# Application Settings
APP_NAME=BACKSTAGE_ENTITY_NAME
